# Supabase Configuration for Top Clients Feedback
# Get these values from your Supabase project dashboard: https://supabase.com/dashboard

# The URL of your Supabase project (Project URL)
# Example: https://your-project-id.supabase.co
VITE_SUPABASE_DATABASE_URL=

# The anonymous key of your Supabase project (API Key - anon/public)
# This is safe to use in client-side code
VITE_SUPABASE_ANON_KEY=

# Optional: Service role key (only if needed for admin operations - keep secure!)
# VITE_SUPABASE_SERVICE_ROLE_KEY=

# Optional: JWT secret (usually not needed in frontend)
# VITE_SUPABASE_JWT_SECRET=

# Note: Create the feedback table in Supabase with this schema:
# 
# CREATE TABLE IF NOT EXISTS top_client_feedback (
#   id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
#   client_email TEXT,
#   client_phone TEXT,
#   client_name TEXT,
#   year TEXT,
#   question1 TEXT NOT NULL,
#   question2 TEXT NOT NULL,
#   general_feedback TEXT NOT NULL,
#   created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
# );
